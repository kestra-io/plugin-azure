kestra:
  repository:
    type: memory
  queue:
    type: memory
  storage:
    type: local
    local:
      base-path: /tmp/unittest
  variables:
    globals:
      azure:
        blobs:
          endpoint: "https://unittestkt.blob.core.windows.net"
          container: tasks
